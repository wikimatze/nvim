let g:ale_open_list = 1     " opens the loclist if there are errors
let g:ale_lint_on_enter = 0 " don't lint when opening a file
let g:ale_set_quickfix = 1  " print messages in quickfix instead of windows location

