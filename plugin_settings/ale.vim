let g:ale_enabled = 0       " ale is disabled by default
let g:ale_open_list = 0     " don't open the loclist if there are errors (they are shown ones you are over the line)
let g:ale_lint_on_enter = 0 " don't lint when opening a file
let g:ale_set_quickfix = 1  " print messages in quickfix instead of windows location

